'use client'

export default function SamplesPhase() {
  return (
    <div className="glass-card p-12 text-center">
      <div className="text-4xl mb-4">📦</div>
      <h2 className="text-xl font-heading text-workspace-text mb-2">
        Request Samples
      </h2>
      <p className="text-workspace-muted text-sm max-w-md mx-auto mb-4">
        Once you have shortlisted suppliers, request product samples to evaluate
        quality before committing to a full order.
      </p>
      <span className="inline-block text-xs px-3 py-1 bg-gold/10 text-gold rounded-full border border-gold/20">
        Coming Soon
      </span>
    </div>
  )
}
